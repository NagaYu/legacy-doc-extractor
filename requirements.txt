# Unstructured document extraction tool for legacy industries
# Targets Python 3.10+

# Data shaping / display
pandas>=2.0.0

# Schema definition / validation (the core of this tool)
pydantic>=2.6.0

# Structured extraction via LLM (Anthropic SDK / uses the AsyncAnthropic async client)
# Note: the API key is read from the ANTHROPIC_API_KEY environment variable
anthropic>=0.49.0

# Exponential-backoff retries to avoid rate limits
tenacity>=8.2.0
